import React from 'react';

export const Chart = ({ data, children, className = '' }: any) => (
  <div className={`w-full h-[300px] flex items-center justify-center bg-gray-50 rounded-lg ${className}`}>
    Chart Placeholder
  </div>
);
